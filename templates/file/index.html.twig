<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}File actions
			{% endblock %}
			{# Twig block 'title': Allows child templates to override the page title dynamically. #}
		</title>
		<link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
		{% block stylesheets %}
			{{ vite_entry_link_tags('appmain') }}
			{# Twig block 'stylesheets': Used to include CSS assets dynamically via Vite. #}
		{% endblock %}

		{% block javascripts %}
			{{ vite_entry_script_tags('appmain') }}
			{# Twig block 'javascripts': Used to include JavaScript assets dynamically via Vite. #}
		{% endblock %}
	</head>
	<body>
		{% block body %}
			<div class="main-grid">
				<h1>File actions</h1>
				<div class="list-layout">
					<button type="button" class="btn btn-primary btn-resize">Resize</button>
					<button type="button" disabled class="btn btn-secondary">Convert extension</button>
					<button type="button" disabled class="btn btn-success">Compress</button>
					<button type="button" disabled class="btn btn-danger">Sepia</button>
					<button type="button" disabled class="btn btn-warning">Rotate</button>
				</div>
			</div>
			{# Twig block 'body': Defines the main content area, which can be overridden in child templates. #}
		{% endblock %}
	</body>
</html>
